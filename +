function guardarUser(nombre,pass) {
  //CREAMOS JSON con array d objs dentro 
  añadir = {
    "Usuarios": [
      {"nombre:": "HijodePuta", "pass": "cabron" }
    ]
  };
  //stringificamos y subimos a localstorage
  localStorage.setItem("Usuarios", JSON.stringify(añadir));

  //para obtener datos
  getObj = JSON.parse(localStorage.getItem("Usuarios")); 

  //MODIF Datos
  añadido = {"nombre":"Mamon", "pass":"Comenabos"};
  getObj.usuarios.push(añadido); 
  localStorage.setItem("Usuarios",JSON.stringify(getObj));

  //Descargamos el modificado:
  getObj2 = JSON.parse(localStorage.getItem("Usuarios")); 
}
